import React from 'react';
import PropTypes from 'prop-types';

const Temps = () => {
    return (
        <div>
            
        </div>
    );
};

Temps.propTypes = {
    
};

export default Temps;